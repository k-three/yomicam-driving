{
  "name": "yomicam-driving-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "typescript": "^5.9.0",
    "vite": "^8.2.2",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "firebase": "^12.18.0"
  }
}
